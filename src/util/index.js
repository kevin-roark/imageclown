
const Easing = require('./easing')
const { futil } = require('./util')
const glob = require('./glob')
const identify = require('./identify')
const ify = require('./ify')
const randomWeights = require('./random-weights')

module.exports = {
  Easing,
  futil,
  glob,
  identify,
  ify,
  randomWeights
}
